#!/bin/bash
# Assume ${temp.dir} in build.xml file set to /tmp/webapp, in ${user.dir}/local.build.properties
~/Tools/appengine-java-sdk-1.4.0/bin/appcfg.sh --email=dominique.derrien@gmail.com update /tmp/webapp/appengine-stage
