#!/bin/bash
# Assume ${temp.dir} in build.xml file set to /tmp/webapp, in ${user.dir}/local.build.properties
./bin/appengine-java-sdk-1.3.4/bin/appcfg.sh --email=dominique.derrien@gmail.com update /tmp/webapp/appengine-stage
